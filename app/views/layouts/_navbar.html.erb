<nav class="navbar navbar-expand-sm bg-primary border-bottom border-body navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Fakebook</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a href="<%= posts_path %>" class="nav-link <%= "active" if controller.controller_name == "posts" %>">Posts</a></li>
        <li class="nav-item"><a href="<%= users_path %>" class="nav-link <%= "active" if controller.controller_name == "users" %>">User Directory</a></li>
      </ul>
      <%= form_with url: destroy_user_session_path, method: :delete, class: "nav-item" do |f| %>
        <%= f.submit "Log Out", class: "nav-link text-white" %>
      <% end %>
    </div>
  </div>
</nav>