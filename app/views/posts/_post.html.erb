<div class="card">
  <div class="card-body">
    <div class="card-text">
      <p><%= post.user.profile.name %></p>
      <p><%= post.created_at.strftime('%B %-d, %Y %l:%M %p') %></p>
      <p><%= post.content %></p>
    </div>
  </div>
</div>